{
//
    function fomatString(input: string, toUpper?: boolean) : string {
        return toUpper ? input.toUpperCase() : input.toLowerCase();
    }
    const Result1 = fomatString("Parvez Mahamud", true);
    console.log(Result1);

//
}